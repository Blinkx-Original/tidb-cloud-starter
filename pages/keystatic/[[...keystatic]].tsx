// pages/keystatic/[[...keystatic]].tsx
// Admin UI for Keystatic (Pages Router)
import { makePage } from '@keystatic/next/ui/pages';
import config from '../../keystatic.config';
export default makePage(config);
